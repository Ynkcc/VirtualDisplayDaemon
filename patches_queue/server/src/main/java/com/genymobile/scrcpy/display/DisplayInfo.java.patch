diff --git a/server/src/main/java/com/genymobile/scrcpy/display/DisplayInfo.java b/server/src/main/java/com/genymobile/scrcpy/display/DisplayInfo.java
index ea966a3e..286ec947 100644
--- a/server/src/main/java/com/genymobile/scrcpy/display/DisplayInfo.java
+++ b/server/src/main/java/com/genymobile/scrcpy/display/DisplayInfo.java
@@ -10,10 +10,23 @@ public final class DisplayInfo {
     private final int flags;
     private final int dpi;
     private final String uniqueId;
+    private final int mirrorDisplayId;
+    private final boolean owned;
+    private final String ownerPackageName;
+    private final int ownerUid;
 
     public static final int FLAG_SUPPORTS_PROTECTED_BUFFERS = 0x00000001;
 
     public DisplayInfo(int displayId, Size size, int rotation, int layerStack, int flags, int dpi, String uniqueId) {
+        this(displayId, size, rotation, layerStack, flags, dpi, uniqueId, -1, false, null, 0);
+    }
+
+    public DisplayInfo(int displayId, Size size, int rotation, int layerStack, int flags, int dpi, String uniqueId, int mirrorDisplayId, boolean owned) {
+        this(displayId, size, rotation, layerStack, flags, dpi, uniqueId, mirrorDisplayId, owned, null, 0);
+    }
+
+    public DisplayInfo(int displayId, Size size, int rotation, int layerStack, int flags, int dpi, String uniqueId,
+                       int mirrorDisplayId, boolean owned, String ownerPackageName, int ownerUid) {
         this.displayId = displayId;
         this.size = size;
         this.rotation = rotation;
@@ -21,6 +34,10 @@ public final class DisplayInfo {
         this.flags = flags;
         this.dpi = dpi;
         this.uniqueId = uniqueId;
+        this.mirrorDisplayId = mirrorDisplayId;
+        this.owned = owned;
+        this.ownerPackageName = ownerPackageName;
+        this.ownerUid = ownerUid;
     }
 
     public int getDisplayId() {
@@ -50,4 +67,20 @@ public final class DisplayInfo {
     public String getUniqueId() {
         return uniqueId;
     }
+
+    public int getMirrorDisplayId() {
+        return mirrorDisplayId;
+    }
+
+    public boolean isOwned() {
+        return owned;
+    }
+
+    public String getOwnerPackageName() {
+        return ownerPackageName;
+    }
+
+    public int getOwnerUid() {
+        return ownerUid;
+    }
 }
