diff --git a/server/src/main/java/com/genymobile/scrcpy/control/DeviceMessage.java b/server/src/main/java/com/genymobile/scrcpy/control/DeviceMessage.java
index 079a7a04..29b07494 100644
--- a/server/src/main/java/com/genymobile/scrcpy/control/DeviceMessage.java
+++ b/server/src/main/java/com/genymobile/scrcpy/control/DeviceMessage.java
@@ -1,6 +1,8 @@
 package com.genymobile.scrcpy.control;
 
-public final class DeviceMessage {
+import com.genymobile.scrcpy.daemon.control.ExtensionCarrier;
+
+public final class DeviceMessage implements ExtensionCarrier {
 
     public static final int TYPE_CLIPBOARD = 0;
     public static final int TYPE_ACK_CLIPBOARD = 1;
@@ -12,6 +14,10 @@ public final class DeviceMessage {
     private int id;
     private byte[] data;
 
+    // Opaque payload attached by daemon-mode extensions. Null for upstream
+    // messages; the daemon writer casts it to a daemon DTO.
+    private Object extensionPayload;
+
     private DeviceMessage() {
     }
 
@@ -37,6 +43,12 @@ public final class DeviceMessage {
         return event;
     }
 
+    public static DeviceMessage createEmpty(int type) {
+        DeviceMessage event = new DeviceMessage();
+        event.type = type;
+        return event;
+    }
+
     public int getType() {
         return type;
     }
@@ -56,4 +68,12 @@ public final class DeviceMessage {
     public byte[] getData() {
         return data;
     }
+
+    public Object getExtensionPayload() {
+        return extensionPayload;
+    }
+
+    public void setExtensionPayload(Object extensionPayload) {
+        this.extensionPayload = extensionPayload;
+    }
 }
