diff --git a/server/src/main/java/com/genymobile/scrcpy/daemon/control/DaemonDeviceMessageWriter.java b/server/src/main/java/com/genymobile/scrcpy/daemon/control/DaemonDeviceMessageWriter.java
new file mode 100644
index 00000000..f818e9ec
--- /dev/null
+++ b/server/src/main/java/com/genymobile/scrcpy/daemon/control/DaemonDeviceMessageWriter.java
@@ -0,0 +1,71 @@
+package com.genymobile.scrcpy.daemon.control;
+
+import com.genymobile.scrcpy.control.DeviceMessage;
+import com.genymobile.scrcpy.daemon.net.DaemonWire;
+import com.genymobile.scrcpy.display.DisplayInfo;
+import com.genymobile.scrcpy.model.DeviceApp;
+
+import java.io.DataOutputStream;
+import java.io.IOException;
+import java.util.List;
+
+public final class DaemonDeviceMessageWriter {
+
+    private DaemonDeviceMessageWriter() {
+    }
+
+    public static boolean write(DeviceMessage msg, DataOutputStream dos) throws IOException {
+        int type = msg.getType();
+        // 只有 daemon 响应类型会走到这里（由 DeviceMessageWriter 的 default 分支调用），
+        // 因此每条消息都带 DaemonDeviceMessage 载荷。
+        DaemonDeviceMessage dto = DaemonMessages.payload(msg);
+        switch (type) {
+            case DaemonMessages.TYPE_RESPONSE_GENERIC:
+                dos.writeLong(dto.getSequence());
+                dos.writeInt(dto.getStatusCode());
+                dos.writeInt(dto.getDisplayId());
+                DaemonWire.writeString(dos, dto.getText());
+                return true;
+            case DaemonMessages.TYPE_RESPONSE_ACTIVE_DISPLAYS:
+                dos.writeLong(dto.getSequence());
+                int[] ids = dto.getDisplayIds();
+                dos.writeInt(ids.length);
+                for (int id : ids) {
+                    dos.writeInt(id);
+                }
+                return true;
+            case DaemonMessages.TYPE_RESPONSE_ACTIVE_DISPLAY_INFOS:
+                dos.writeLong(dto.getSequence());
+                DisplayInfo[] infos = dto.getDisplayInfos();
+                dos.writeInt(infos.length);
+                for (DisplayInfo info : infos) {
+                    dos.writeInt(info.getDisplayId());
+                    dos.writeInt(info.getSize().getWidth());
+                    dos.writeInt(info.getSize().getHeight());
+                    dos.writeInt(info.getDpi());
+                    dos.writeInt(info.getRotation());
+                    dos.writeInt(info.getMirrorDisplayId());
+                    dos.writeByte(info.isOwned() ? 1 : 0);
+                    dos.writeInt(info.getOwnerUid());
+                    DaemonWire.writeString(dos, info.getOwnerPackageName());
+                }
+                return true;
+            case DaemonMessages.TYPE_RESPONSE_APPS_LIST:
+                dos.writeLong(dto.getSequence());
+                List<DeviceApp> apps = dto.getApps();
+                if (apps == null) {
+                    dos.writeInt(0);
+                    return true;
+                }
+                dos.writeInt(apps.size());
+                for (DeviceApp app : apps) {
+                    DaemonWire.writeString(dos, app.getPackageName());
+                    DaemonWire.writeString(dos, app.getName());
+                    dos.writeByte(app.isSystem() ? 1 : 0);
+                }
+                return true;
+            default:
+                return false;
+        }
+    }
+}
